// Compiled by ClojureScript 1.9.946 {}
goog.provide('idb.IDBOpenDBRequest');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The onblocked event handler of the `idb.IDBOpenDBRequest` interface
 *   the event handler for the blocked event. This event is triggered
 *   the upgradeneeded should be triggered because of a version change
 *   the database is still in use (that is, not closed) somewhere,
 *   after the versionchange event was sent.
 * 
 *   `IDBOpenDBRequest.onblocked = function(event) { ... };`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/IDBOpenDBRequest/onblocked`
 */
idb.IDBOpenDBRequest.onblocked = (function idb$IDBOpenDBRequest$onblocked(this$){
return this$.onblocked();
});
/**
 * Property.
 * 
 *   The onblocked event handler of the `idb.IDBOpenDBRequest` interface
 *   the event handler for the blocked event. This event is triggered
 *   the upgradeneeded should be triggered because of a version change
 *   the database is still in use (that is, not closed) somewhere,
 *   after the versionchange event was sent.
 * 
 *   `IDBOpenDBRequest.onblocked = function(event) { ... };`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/IDBOpenDBRequest/onblocked`
 */
idb.IDBOpenDBRequest.set_onblocked_BANG_ = (function idb$IDBOpenDBRequest$set_onblocked_BANG_(this$,val){
return (this$["onblocked"] = val);
});
/**
 * Property.
 * 
 *   In the following example you can see the onupgradeneeded handler
 *   used to update the database structure if a database with a higher
 *   number is loaded. For a full working example, see our To-do Notifications
 *   (view example live.)
 * 
 *   `IDBOpenDBRequest.onupgradeneeded = function(event) { ... };`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/IDBOpenDBRequest/onupgradeneeded`
 */
idb.IDBOpenDBRequest.onupgradeneeded = (function idb$IDBOpenDBRequest$onupgradeneeded(this$){
return this$.onupgradeneeded();
});
/**
 * Property.
 * 
 *   In the following example you can see the onupgradeneeded handler
 *   used to update the database structure if a database with a higher
 *   number is loaded. For a full working example, see our To-do Notifications
 *   (view example live.)
 * 
 *   `IDBOpenDBRequest.onupgradeneeded = function(event) { ... };`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/IDBOpenDBRequest/onupgradeneeded`
 */
idb.IDBOpenDBRequest.set_onupgradeneeded_BANG_ = (function idb$IDBOpenDBRequest$set_onupgradeneeded_BANG_(this$,val){
return (this$["onupgradeneeded"] = val);
});

//# sourceMappingURL=IDBOpenDBRequest.js.map?rel=1565798855761
