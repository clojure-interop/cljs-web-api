// Compiled by ClojureScript 1.9.946 {}
goog.provide('speech.SpeechSynthesisEvent');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The charIndex read-only property of the `speech.SpeechSynthesisUtterance`
 *   returns the index position of the character in the `speech.SpeechSynthesisUtterance.text`
 *   was being spoken when the event was triggered.
 * 
 *   `event.charIndex;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent/charIndex`
 */
speech.SpeechSynthesisEvent.char_index = (function speech$SpeechSynthesisEvent$char_index(this$){
return this$.charIndex();
});
/**
 * Property.
 * 
 *   The charIndex read-only property of the `speech.SpeechSynthesisUtterance`
 *   returns the index position of the character in the `speech.SpeechSynthesisUtterance.text`
 *   was being spoken when the event was triggered.
 * 
 *   `event.charIndex;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent/charIndex`
 */
speech.SpeechSynthesisEvent.set_char_index_BANG_ = (function speech$SpeechSynthesisEvent$set_char_index_BANG_(this$,val){
return (this$["charIndex"] = val);
});
/**
 * Property.
 * 
 *   The elapsedTime read-only property of the `speech.SpeechSynthesisUtterance`
 *   returns the elapsed time in seconds after the `speech.SpeechSynthesisUtterance.text`
 *   being spoken that the event was triggered at.
 * 
 *   `event.elapsedTime;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent/elapsedTime`
 */
speech.SpeechSynthesisEvent.elapsed_time = (function speech$SpeechSynthesisEvent$elapsed_time(this$){
return this$.elapsedTime();
});
/**
 * Property.
 * 
 *   The elapsedTime read-only property of the `speech.SpeechSynthesisUtterance`
 *   returns the elapsed time in seconds after the `speech.SpeechSynthesisUtterance.text`
 *   being spoken that the event was triggered at.
 * 
 *   `event.elapsedTime;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent/elapsedTime`
 */
speech.SpeechSynthesisEvent.set_elapsed_time_BANG_ = (function speech$SpeechSynthesisEvent$set_elapsed_time_BANG_(this$,val){
return (this$["elapsedTime"] = val);
});
/**
 * Property.
 * 
 *   The name read-only property of the `speech.SpeechSynthesisUtterance`
 *   returns the name associated with certain types of events occuring
 *   the `speech.SpeechSynthesisUtterance.text` is being spoken: the
 *   of the SSML marker reached in the case of a mark event, or the
 *   of boundary reached in the case of a boundary event.
 * 
 *   `event.name;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent/name`
 */
speech.SpeechSynthesisEvent.name = (function speech$SpeechSynthesisEvent$name(this$){
return this$.name();
});
/**
 * Property.
 * 
 *   The name read-only property of the `speech.SpeechSynthesisUtterance`
 *   returns the name associated with certain types of events occuring
 *   the `speech.SpeechSynthesisUtterance.text` is being spoken: the
 *   of the SSML marker reached in the case of a mark event, or the
 *   of boundary reached in the case of a boundary event.
 * 
 *   `event.name;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent/name`
 */
speech.SpeechSynthesisEvent.set_name_BANG_ = (function speech$SpeechSynthesisEvent$set_name_BANG_(this$,val){
return (this$["name"] = val);
});
/**
 * Property.
 * 
 *   The utterance read-only property of the `speech.SpeechSynthesisUtterance`
 *   returns the `speech.SpeechSynthesisUtterance` instance that the
 *   was triggered on.
 * 
 *   `event.utterance;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent/utterance`
 */
speech.SpeechSynthesisEvent.utterance = (function speech$SpeechSynthesisEvent$utterance(this$){
return this$.utterance();
});
/**
 * Property.
 * 
 *   The utterance read-only property of the `speech.SpeechSynthesisUtterance`
 *   returns the `speech.SpeechSynthesisUtterance` instance that the
 *   was triggered on.
 * 
 *   `event.utterance;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent/utterance`
 */
speech.SpeechSynthesisEvent.set_utterance_BANG_ = (function speech$SpeechSynthesisEvent$set_utterance_BANG_(this$,val){
return (this$["utterance"] = val);
});

//# sourceMappingURL=SpeechSynthesisEvent.js.map?rel=1565798857254
