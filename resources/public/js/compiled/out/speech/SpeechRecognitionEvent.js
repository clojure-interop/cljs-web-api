// Compiled by ClojureScript 1.9.946 {}
goog.provide('speech.SpeechRecognitionEvent');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The emma read-only property of the `speech.SpeechRecognitionEvent`
 *   returns an Extensible MultiModal Annotation markup language (EMMA)
 *   XML — representation of the result.
 * 
 *   `var myEmma = event.emma;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognitionEvent/emma`
 */
speech.SpeechRecognitionEvent.emma = (function speech$SpeechRecognitionEvent$emma(this$){
return this$.emma();
});
/**
 * Property.
 * 
 *   The emma read-only property of the `speech.SpeechRecognitionEvent`
 *   returns an Extensible MultiModal Annotation markup language (EMMA)
 *   XML — representation of the result.
 * 
 *   `var myEmma = event.emma;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognitionEvent/emma`
 */
speech.SpeechRecognitionEvent.set_emma_BANG_ = (function speech$SpeechRecognitionEvent$set_emma_BANG_(this$,val){
return (this$["emma"] = val);
});
/**
 * Property.
 * 
 *   The interpretation read-only property of the `speech.SpeechRecognitionEvent`
 *   returns the semantic meaning of what the user said.
 * 
 *   `var myInterpretation = event.interpretation;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognitionEvent/interpretation`
 */
speech.SpeechRecognitionEvent.interpretation = (function speech$SpeechRecognitionEvent$interpretation(this$){
return this$.interpretation();
});
/**
 * Property.
 * 
 *   The interpretation read-only property of the `speech.SpeechRecognitionEvent`
 *   returns the semantic meaning of what the user said.
 * 
 *   `var myInterpretation = event.interpretation;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognitionEvent/interpretation`
 */
speech.SpeechRecognitionEvent.set_interpretation_BANG_ = (function speech$SpeechRecognitionEvent$set_interpretation_BANG_(this$,val){
return (this$["interpretation"] = val);
});
/**
 * Property.
 * 
 *   The resultIndex read-only property of the `speech.SpeechRecognitionEvent`
 *   returns the lowest index value result in the `speech.SpeechRecognitionResultList`
 *   that has actually changed.
 * 
 *   `var myResultIndex = event.resultIndex;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognitionEvent/resultIndex`
 */
speech.SpeechRecognitionEvent.result_index = (function speech$SpeechRecognitionEvent$result_index(this$){
return this$.resultIndex();
});
/**
 * Property.
 * 
 *   The resultIndex read-only property of the `speech.SpeechRecognitionEvent`
 *   returns the lowest index value result in the `speech.SpeechRecognitionResultList`
 *   that has actually changed.
 * 
 *   `var myResultIndex = event.resultIndex;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognitionEvent/resultIndex`
 */
speech.SpeechRecognitionEvent.set_result_index_BANG_ = (function speech$SpeechRecognitionEvent$set_result_index_BANG_(this$,val){
return (this$["resultIndex"] = val);
});
/**
 * Property.
 * 
 *   The results read-only property of the `speech.SpeechRecognitionEvent`
 *   returns a `speech.SpeechRecognitionResultList` object representing
 *   the speech recognition results for the current session.
 * 
 *   `var myResults = event.results;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognitionEvent/results`
 */
speech.SpeechRecognitionEvent.results = (function speech$SpeechRecognitionEvent$results(this$){
return this$.results();
});
/**
 * Property.
 * 
 *   The results read-only property of the `speech.SpeechRecognitionEvent`
 *   returns a `speech.SpeechRecognitionResultList` object representing
 *   the speech recognition results for the current session.
 * 
 *   `var myResults = event.results;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognitionEvent/results`
 */
speech.SpeechRecognitionEvent.set_results_BANG_ = (function speech$SpeechRecognitionEvent$set_results_BANG_(this$,val){
return (this$["results"] = val);
});

//# sourceMappingURL=SpeechRecognitionEvent.js.map?rel=1565798858990
