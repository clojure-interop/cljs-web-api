// Compiled by ClojureScript 1.9.946 {}
goog.provide('vr.VRDisplayCapabilities');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The canPresent read-only property of the `vr.VRDisplayCapabilities`
 *   returns a `web.Boolean` stating whether the VR display is capable
 *   presenting content (e.g. through an HMD).
 * 
 *   `var canIPresent = vrDisplayCapabilitiesInstance.canPresent;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/canPresent`
 */
vr.VRDisplayCapabilities.can_present = (function vr$VRDisplayCapabilities$can_present(this$){
return this$.canPresent();
});
/**
 * Property.
 * 
 *   The canPresent read-only property of the `vr.VRDisplayCapabilities`
 *   returns a `web.Boolean` stating whether the VR display is capable
 *   presenting content (e.g. through an HMD).
 * 
 *   `var canIPresent = vrDisplayCapabilitiesInstance.canPresent;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/canPresent`
 */
vr.VRDisplayCapabilities.set_can_present_BANG_ = (function vr$VRDisplayCapabilities$set_can_present_BANG_(this$,val){
return (this$["canPresent"] = val);
});
/**
 * Property.
 * 
 *   The hasExternalDisplay read-only property of the `vr.VRDisplayCapabilities`
 *   returns a `web.Boolean` stating whether the VR display is separate
 *   the device's primary display.
 * 
 *   `var hasAnExternalDisplay = vrDisplayCapabilitiesInstance.hasExternalDisplay;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/hasExternalDisplay`
 */
vr.VRDisplayCapabilities.has_external_display = (function vr$VRDisplayCapabilities$has_external_display(this$){
return this$.hasExternalDisplay();
});
/**
 * Property.
 * 
 *   The hasExternalDisplay read-only property of the `vr.VRDisplayCapabilities`
 *   returns a `web.Boolean` stating whether the VR display is separate
 *   the device's primary display.
 * 
 *   `var hasAnExternalDisplay = vrDisplayCapabilitiesInstance.hasExternalDisplay;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/hasExternalDisplay`
 */
vr.VRDisplayCapabilities.set_has_external_display_BANG_ = (function vr$VRDisplayCapabilities$set_has_external_display_BANG_(this$,val){
return (this$["hasExternalDisplay"] = val);
});
/**
 * Property.
 * 
 *   The hasOrientation read-only property of the `vr.VRDisplayCapabilities`
 *   returns a `web.Boolean` stating whether the VR display can track
 *   return orientation information.
 * 
 *   `var hasItGotOrientation = vrDisplayCapabilitiesInstance.hasOrientation;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/hasOrientation`
 */
vr.VRDisplayCapabilities.has_orientation = (function vr$VRDisplayCapabilities$has_orientation(this$){
return this$.hasOrientation();
});
/**
 * Property.
 * 
 *   The hasOrientation read-only property of the `vr.VRDisplayCapabilities`
 *   returns a `web.Boolean` stating whether the VR display can track
 *   return orientation information.
 * 
 *   `var hasItGotOrientation = vrDisplayCapabilitiesInstance.hasOrientation;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/hasOrientation`
 */
vr.VRDisplayCapabilities.set_has_orientation_BANG_ = (function vr$VRDisplayCapabilities$set_has_orientation_BANG_(this$,val){
return (this$["hasOrientation"] = val);
});
/**
 * Property.
 * 
 *   The hasPosition read-only property of the `vr.VRDisplayCapabilities`
 *   returns a `web.Boolean` stating whether the VR display can track
 *   return position information.
 * 
 *   `var hasItGotPosition = vrDisplayCapabilitiesInstance.hasPosition;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/hasPosition`
 */
vr.VRDisplayCapabilities.has_position = (function vr$VRDisplayCapabilities$has_position(this$){
return this$.hasPosition();
});
/**
 * Property.
 * 
 *   The hasPosition read-only property of the `vr.VRDisplayCapabilities`
 *   returns a `web.Boolean` stating whether the VR display can track
 *   return position information.
 * 
 *   `var hasItGotPosition = vrDisplayCapabilitiesInstance.hasPosition;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/hasPosition`
 */
vr.VRDisplayCapabilities.set_has_position_BANG_ = (function vr$VRDisplayCapabilities$set_has_position_BANG_(this$,val){
return (this$["hasPosition"] = val);
});
/**
 * Property.
 * 
 *   The maxLayers read-only property of the `vr.VRDisplayCapabilities`
 *   returns a number indicating the maximum number of `vr.VRLayer`s
 *   the VR display can present at once (e.g. the maximum length of
 *   array that `web.Display.requestPresent()` can accept.)
 * 
 *   `var maximumLayers = vrDisplayCapabilitiesInstance.maxLayers;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/maxLayers`
 */
vr.VRDisplayCapabilities.max_layers = (function vr$VRDisplayCapabilities$max_layers(this$){
return this$.maxLayers();
});
/**
 * Property.
 * 
 *   The maxLayers read-only property of the `vr.VRDisplayCapabilities`
 *   returns a number indicating the maximum number of `vr.VRLayer`s
 *   the VR display can present at once (e.g. the maximum length of
 *   array that `web.Display.requestPresent()` can accept.)
 * 
 *   `var maximumLayers = vrDisplayCapabilitiesInstance.maxLayers;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/VRDisplayCapabilities/maxLayers`
 */
vr.VRDisplayCapabilities.set_max_layers_BANG_ = (function vr$VRDisplayCapabilities$set_max_layers_BANG_(this$,val){
return (this$["maxLayers"] = val);
});

//# sourceMappingURL=VRDisplayCapabilities.js.map?rel=1565798811263
