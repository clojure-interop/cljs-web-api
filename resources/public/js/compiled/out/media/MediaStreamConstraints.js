// Compiled by ClojureScript 1.9.946 {}
goog.provide('media.MediaStreamConstraints');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The `media.MediaStreamConstraints` dictionary's audio property
 *   used to indicate what kind of audio track, if any, should be
 *   in the `media.MediaStream` returned by a call to `web.getUserMedia()`.
 * 
 *   `var audioConstraints = true | false | MediaTrackConstraints;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MediaStreamConstraints/audio`
 */
media.MediaStreamConstraints.audio = (function media$MediaStreamConstraints$audio(this$){
return this$.audio();
});
/**
 * Property.
 * 
 *   The `media.MediaStreamConstraints` dictionary's audio property
 *   used to indicate what kind of audio track, if any, should be
 *   in the `media.MediaStream` returned by a call to `web.getUserMedia()`.
 * 
 *   `var audioConstraints = true | false | MediaTrackConstraints;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MediaStreamConstraints/audio`
 */
media.MediaStreamConstraints.set_audio_BANG_ = (function media$MediaStreamConstraints$set_audio_BANG_(this$,val){
return (this$["audio"] = val);
});
/**
 * Property.
 * 
 *   The `media.MediaStreamConstraints` dictionary's video property
 *   used to indicate what kind of video track, if any, should be
 *   in the `media.MediaStream` returned by a call to `web.getUserMedia()`.
 * 
 *   `var videoConstraints = true | false | MediaTrackConstraints;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MediaStreamConstraints/video`
 */
media.MediaStreamConstraints.video = (function media$MediaStreamConstraints$video(this$){
return this$.video();
});
/**
 * Property.
 * 
 *   The `media.MediaStreamConstraints` dictionary's video property
 *   used to indicate what kind of video track, if any, should be
 *   in the `media.MediaStream` returned by a call to `web.getUserMedia()`.
 * 
 *   `var videoConstraints = true | false | MediaTrackConstraints;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MediaStreamConstraints/video`
 */
media.MediaStreamConstraints.set_video_BANG_ = (function media$MediaStreamConstraints$set_video_BANG_(this$,val){
return (this$["video"] = val);
});

//# sourceMappingURL=MediaStreamConstraints.js.map?rel=1565798808179
