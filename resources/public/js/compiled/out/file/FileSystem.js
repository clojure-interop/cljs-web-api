// Compiled by ClojureScript 1.9.946 {}
goog.provide('file.FileSystem');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The read-only name property of the `file.FileSystem` interface
 *   the file system's name. This `web.USVString` is unique among
 *   file systems currently exposed by the File and Directory Entries
 * 
 *   `var fsName = FileSystem.name;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/FileSystem/name`
 */
file.FileSystem.name = (function file$FileSystem$name(this$){
return this$.name();
});
/**
 * Property.
 * 
 *   The read-only name property of the `file.FileSystem` interface
 *   the file system's name. This `web.USVString` is unique among
 *   file systems currently exposed by the File and Directory Entries
 * 
 *   `var fsName = FileSystem.name;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/FileSystem/name`
 */
file.FileSystem.set_name_BANG_ = (function file$FileSystem$set_name_BANG_(this$,val){
return (this$["name"] = val);
});
/**
 * Property.
 * 
 *   The read-only root property of the `file.FileSystem` interface
 *   a `file.FileSystemDirectoryEntry` object representing the root
 *   of the file system, for use with the File and Directory Entries
 * 
 *   `var rootDirEnt = FileSystem.root;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/FileSystem/root`
 */
file.FileSystem.root = (function file$FileSystem$root(this$){
return this$.root();
});
/**
 * Property.
 * 
 *   The read-only root property of the `file.FileSystem` interface
 *   a `file.FileSystemDirectoryEntry` object representing the root
 *   of the file system, for use with the File and Directory Entries
 * 
 *   `var rootDirEnt = FileSystem.root;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/FileSystem/root`
 */
file.FileSystem.set_root_BANG_ = (function file$FileSystem$set_root_BANG_(this$,val){
return (this$["root"] = val);
});

//# sourceMappingURL=FileSystem.js.map?rel=1565798815659
