// Compiled by ClojureScript 1.9.946 {}
goog.provide('css.CSSRule');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The cssText property of the `css.CSSRule` interface returns the
 *   text of a `css.CSSStyleSheet` style-rule.
 * 
 *   `string = cssRule.cssText`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/CSSRule/cssText`
 */
css.CSSRule.css_text = (function css$CSSRule$css_text(this$){
return this$.cssText();
});
/**
 * Property.
 * 
 *   The cssText property of the `css.CSSRule` interface returns the
 *   text of a `css.CSSStyleSheet` style-rule.
 * 
 *   `string = cssRule.cssText`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/CSSRule/cssText`
 */
css.CSSRule.set_css_text_BANG_ = (function css$CSSRule$set_css_text_BANG_(this$,val){
return (this$["cssText"] = val);
});
/**
 * Property.
 * 
 *   The parentStyleSheet property of the `css.CSSRule` interface
 *   the `web.StyleSheet` object in which the current rule is defined.
 * 
 *   `var stylesheet = cssRule.parentStyleSheet`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/CSSRule/parentStyleSheet`
 */
css.CSSRule.parent_style_sheet = (function css$CSSRule$parent_style_sheet(this$){
return this$.parentStyleSheet();
});
/**
 * Property.
 * 
 *   The parentStyleSheet property of the `css.CSSRule` interface
 *   the `web.StyleSheet` object in which the current rule is defined.
 * 
 *   `var stylesheet = cssRule.parentStyleSheet`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/CSSRule/parentStyleSheet`
 */
css.CSSRule.set_parent_style_sheet_BANG_ = (function css$CSSRule$set_parent_style_sheet_BANG_(this$,val){
return (this$["parentStyleSheet"] = val);
});

//# sourceMappingURL=CSSRule.js.map?rel=1565798823492
