// Compiled by ClojureScript 1.9.946 {}
goog.provide('svg.SVGAElement');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The SVGAElement.target read-only property of `svg.SVGAElement`
 *   an `svg.SVGAnimatedString` object that specifies the portion
 *   a target window, frame, pane into which a document is to be opened
 *   a link is activated.
 * 
 *   `myLink.target = 'value';`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SVGAElement/target`
 */
svg.SVGAElement.target = (function svg$SVGAElement$target(this$){
return this$.target();
});
/**
 * Property.
 * 
 *   The SVGAElement.target read-only property of `svg.SVGAElement`
 *   an `svg.SVGAnimatedString` object that specifies the portion
 *   a target window, frame, pane into which a document is to be opened
 *   a link is activated.
 * 
 *   `myLink.target = 'value';`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/SVGAElement/target`
 */
svg.SVGAElement.set_target_BANG_ = (function svg$SVGAElement$set_target_BANG_(this$,val){
return (this$["target"] = val);
});

//# sourceMappingURL=SVGAElement.js.map?rel=1565798851485
