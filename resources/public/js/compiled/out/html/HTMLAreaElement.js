// Compiled by ClojureScript 1.9.946 {}
goog.provide('html.HTMLAreaElement');
goog.require('cljs.core');
/**
 * Method.
 * 
 *   The HTMLHyperlinkElementUtils.toString() stringifier method returns
 *   `web.USVString` containing the whole URL. It is a read-only version
 *   `html.HTMLHyperlinkElementUtils.href`.
 * 
 *   `string = object.toString();`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/toString`
 */
html.HTMLAreaElement.to_string = (function html$HTMLAreaElement$to_string(this$){
return this$.toString();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.hash property returns a `dom.DOMString`
 *   a '#' followed by the fragment identifier of the URL.
 * 
 *   `string = object.hash;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/hash`
 */
html.HTMLAreaElement.hash = (function html$HTMLAreaElement$hash(this$){
return this$.hash();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.hash property returns a `dom.DOMString`
 *   a '#' followed by the fragment identifier of the URL.
 * 
 *   `string = object.hash;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/hash`
 */
html.HTMLAreaElement.set_hash_BANG_ = (function html$HTMLAreaElement$set_hash_BANG_(this$,val){
return (this$["hash"] = val);
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.host property is a `web.USVString`
 *   the host, that is the hostname, and then, if the port of the
 *   is nonempty, a ':', and the port of the URL.
 * 
 *   `string = object.host;
 *   object.host = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/host`
 */
html.HTMLAreaElement.host = (function html$HTMLAreaElement$host(this$){
return this$.host();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.host property is a `web.USVString`
 *   the host, that is the hostname, and then, if the port of the
 *   is nonempty, a ':', and the port of the URL.
 * 
 *   `string = object.host;
 *   object.host = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/host`
 */
html.HTMLAreaElement.set_host_BANG_ = (function html$HTMLAreaElement$set_host_BANG_(this$,val){
return (this$["host"] = val);
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.hostname property is a `web.USVString`
 *   the domain of the URL.
 * 
 *   `string = object.hostname;
 *   object.hostname = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/hostname`
 */
html.HTMLAreaElement.hostname = (function html$HTMLAreaElement$hostname(this$){
return this$.hostname();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.hostname property is a `web.USVString`
 *   the domain of the URL.
 * 
 *   `string = object.hostname;
 *   object.hostname = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/hostname`
 */
html.HTMLAreaElement.set_hostname_BANG_ = (function html$HTMLAreaElement$set_hostname_BANG_(this$,val){
return (this$["hostname"] = val);
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.href property is a `web.USVString`
 *   the whole URL.
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/href`
 */
html.HTMLAreaElement.href = (function html$HTMLAreaElement$href(this$){
return this$.href();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.href property is a `web.USVString`
 *   the whole URL.
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/href`
 */
html.HTMLAreaElement.set_href_BANG_ = (function html$HTMLAreaElement$set_href_BANG_(this$,val){
return (this$["href"] = val);
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.origin read-only property is a
 *   containing the Unicode serialization of the origin of the represented
 * 
 *   `string = object.origin;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/origin`
 */
html.HTMLAreaElement.origin = (function html$HTMLAreaElement$origin(this$){
return this$.origin();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.password property is a `web.USVString`
 *   the password specified before the domain name.
 * 
 *   `string = object.password;
 *   object.password = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/password`
 */
html.HTMLAreaElement.password = (function html$HTMLAreaElement$password(this$){
return this$.password();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.pathname property is a `web.USVString`
 *   an initial '/' followed by the path of the URL (or the empty
 *   if there is no path).
 * 
 *   `string = object.pathname;
 *   object.pathname = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/pathname`
 */
html.HTMLAreaElement.pathname = (function html$HTMLAreaElement$pathname(this$){
return this$.pathname();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.pathname property is a `web.USVString`
 *   an initial '/' followed by the path of the URL (or the empty
 *   if there is no path).
 * 
 *   `string = object.pathname;
 *   object.pathname = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/pathname`
 */
html.HTMLAreaElement.set_pathname_BANG_ = (function html$HTMLAreaElement$set_pathname_BANG_(this$,val){
return (this$["pathname"] = val);
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.port property is a `web.USVString`
 *   the port number of the URL. If the URL does not contain an explicit
 *   number, it will be set to ''.
 * 
 *   `string = object.port;
 *   object.port = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/port`
 */
html.HTMLAreaElement.port = (function html$HTMLAreaElement$port(this$){
return this$.port();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.port property is a `web.USVString`
 *   the port number of the URL. If the URL does not contain an explicit
 *   number, it will be set to ''.
 * 
 *   `string = object.port;
 *   object.port = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/port`
 */
html.HTMLAreaElement.set_port_BANG_ = (function html$HTMLAreaElement$set_port_BANG_(this$,val){
return (this$["port"] = val);
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.protocol property is a `web.USVString`
 *   the protocol scheme of the URL, including the final ':'.
 * 
 *   `string = object.protocol;
 *   object.protocol = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/protocol`
 */
html.HTMLAreaElement.protocol = (function html$HTMLAreaElement$protocol(this$){
return this$.protocol();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.protocol property is a `web.USVString`
 *   the protocol scheme of the URL, including the final ':'.
 * 
 *   `string = object.protocol;
 *   object.protocol = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/protocol`
 */
html.HTMLAreaElement.set_protocol_BANG_ = (function html$HTMLAreaElement$set_protocol_BANG_(this$,val){
return (this$["protocol"] = val);
});
/**
 * Property.
 * 
 *   The HTMLAreaElement.referrerPolicy property reflect the HTML
 *   attribute of the `web.<area>` element defining which referrer
 *   sent when fetching the resource.
 * 
 *   `refStr = areaElt.referrerPolicy;
 *   areaElt.referrerPolicy = refStr;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLAreaElement/referrerPolicy`
 */
html.HTMLAreaElement.referrer_policy = (function html$HTMLAreaElement$referrer_policy(this$){
return this$.referrerPolicy();
});
/**
 * Property.
 * 
 *   The HTMLAreaElement.referrerPolicy property reflect the HTML
 *   attribute of the `web.<area>` element defining which referrer
 *   sent when fetching the resource.
 * 
 *   `refStr = areaElt.referrerPolicy;
 *   areaElt.referrerPolicy = refStr;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLAreaElement/referrerPolicy`
 */
html.HTMLAreaElement.set_referrer_policy_BANG_ = (function html$HTMLAreaElement$set_referrer_policy_BANG_(this$,val){
return (this$["referrerPolicy"] = val);
});
/**
 * Property.
 * 
 *   The HTMLAreaElement.rel property reflects the rel attribute.
 *   is a `dom.DOMString` containing a space-separated list of link
 *   indicating the relationship between the resource represented
 *   the `web.<a>` element and the current document.
 * 
 *   `var relstr = areaElt.rel;
 *   areaElt.rel = relstr;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLAreaElement/rel`
 */
html.HTMLAreaElement.rel = (function html$HTMLAreaElement$rel(this$){
return this$.rel();
});
/**
 * Property.
 * 
 *   The HTMLAreaElement.rel property reflects the rel attribute.
 *   is a `dom.DOMString` containing a space-separated list of link
 *   indicating the relationship between the resource represented
 *   the `web.<a>` element and the current document.
 * 
 *   `var relstr = areaElt.rel;
 *   areaElt.rel = relstr;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLAreaElement/rel`
 */
html.HTMLAreaElement.set_rel_BANG_ = (function html$HTMLAreaElement$set_rel_BANG_(this$,val){
return (this$["rel"] = val);
});
/**
 * Property.
 * 
 *   The HTMLAreaElement.relList read-only property reflects the rel
 *   It is a live `dom.DOMTokenList` containing the set of link types
 *   the relationship between the resource represented by the `web.<area>`
 *   and the current document.
 * 
 *   `var relstr = areaElt.relList;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLAreaElement/relList`
 */
html.HTMLAreaElement.rel_list = (function html$HTMLAreaElement$rel_list(this$){
return this$.relList();
});
/**
 * Property.
 * 
 *   The HTMLAreaElement.relList read-only property reflects the rel
 *   It is a live `dom.DOMTokenList` containing the set of link types
 *   the relationship between the resource represented by the `web.<area>`
 *   and the current document.
 * 
 *   `var relstr = areaElt.relList;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLAreaElement/relList`
 */
html.HTMLAreaElement.set_rel_list_BANG_ = (function html$HTMLAreaElement$set_rel_list_BANG_(this$,val){
return (this$["relList"] = val);
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.search property is a search string,
 *   called a query string, that is `web.USVString` containing a '?'
 *   by the parameters of the URL.
 * 
 *   `string = object.search;
 *   object.search = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/search`
 */
html.HTMLAreaElement.search = (function html$HTMLAreaElement$search(this$){
return this$.search();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.search property is a search string,
 *   called a query string, that is `web.USVString` containing a '?'
 *   by the parameters of the URL.
 * 
 *   `string = object.search;
 *   object.search = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/search`
 */
html.HTMLAreaElement.set_search_BANG_ = (function html$HTMLAreaElement$set_search_BANG_(this$,val){
return (this$["search"] = val);
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.username property is a `web.USVString`
 *   the username specified before the domain name.
 * 
 *   `string = object.username;
 *   object.username = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/username`
 */
html.HTMLAreaElement.username = (function html$HTMLAreaElement$username(this$){
return this$.username();
});
/**
 * Property.
 * 
 *   The HTMLHyperlinkElementUtils.username property is a `web.USVString`
 *   the username specified before the domain name.
 * 
 *   `string = object.username;
 *   object.username = string;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLHyperlinkElementUtils/username`
 */
html.HTMLAreaElement.set_username_BANG_ = (function html$HTMLAreaElement$set_username_BANG_(this$,val){
return (this$["username"] = val);
});

//# sourceMappingURL=HTMLAreaElement.js.map?rel=1565798854059
