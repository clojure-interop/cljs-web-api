// Compiled by ClojureScript 1.9.946 {}
goog.provide('html.HTMLDialogElement');
goog.require('cljs.core');
/**
 * Method.
 * 
 *   Void.
 * 
 *   `dialogInstance.close(returnValue);`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/close`
 */
html.HTMLDialogElement.close = (function html$HTMLDialogElement$close(this$,return_value){
return this$.close(return_value);
});
/**
 * Method.
 * 
 *   The show() method of the `html.HTMLDialogElement` interface displays
 *   dialog modelessly, i.e. still allowing interaction with content
 *   of the dialog.
 * 
 *   `dialogInstance.show();`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/show`
 */
html.HTMLDialogElement.show = (function html$HTMLDialogElement$show(this$){
return this$.show();
});
/**
 * Method.
 * 
 *   The showModal() method of the `html.HTMLDialogElement` interface
 *   the dialog as a modal, over the top of any other dialogs that
 *   be present. It displays into the top layer, along with a ::backdrop
 *   Interaction outside the dialog is blocked and the content outside
 *   is rendered inert.
 * 
 *   `dialogInstance.showModal();`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/showModal`
 */
html.HTMLDialogElement.show_modal = (function html$HTMLDialogElement$show_modal(this$){
return this$.showModal();
});
/**
 * Property.
 * 
 *   A `web.Boolean` representing the state of the open HTML attribute.
 *   means it is set, and therefore the dialog is shown. false means
 *   not set, and therefore the dialog is not shown.
 * 
 *   `dialogInstance.open = true;
 *   var myOpenValue = dialogInstance.open;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/open`
 */
html.HTMLDialogElement.open = (function html$HTMLDialogElement$open(this$){
return this$.open();
});
/**
 * Property.
 * 
 *   A `web.Boolean` representing the state of the open HTML attribute.
 *   means it is set, and therefore the dialog is shown. false means
 *   not set, and therefore the dialog is not shown.
 * 
 *   `dialogInstance.open = true;
 *   var myOpenValue = dialogInstance.open;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/open`
 */
html.HTMLDialogElement.set_open_BANG_ = (function html$HTMLDialogElement$set_open_BANG_(this$,val){
return (this$["open"] = val);
});
/**
 * Property.
 * 
 *   The returnValue property of the `html.HTMLDialogElement` interface
 *   or sets the return value for the <dialog>, usually to indicate
 *   button the user pressed to close it.
 * 
 *   `dialogInstance.returnValue = 'myReturnValue';
 *   var myReturnValue = dialogInstance.returnValue;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/returnValue`
 */
html.HTMLDialogElement.return_value = (function html$HTMLDialogElement$return_value(this$){
return this$.returnValue();
});
/**
 * Property.
 * 
 *   The returnValue property of the `html.HTMLDialogElement` interface
 *   or sets the return value for the <dialog>, usually to indicate
 *   button the user pressed to close it.
 * 
 *   `dialogInstance.returnValue = 'myReturnValue';
 *   var myReturnValue = dialogInstance.returnValue;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/returnValue`
 */
html.HTMLDialogElement.set_return_value_BANG_ = (function html$HTMLDialogElement$set_return_value_BANG_(this$,val){
return (this$["returnValue"] = val);
});

//# sourceMappingURL=HTMLDialogElement.js.map?rel=1565798855332
