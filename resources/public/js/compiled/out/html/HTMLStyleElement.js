// Compiled by ClojureScript 1.9.946 {}
goog.provide('html.HTMLStyleElement');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The HTMLStyleElement.media property specifies the intended destination
 *   for style information.
 * 
 *   `medium = style.media
 *   style.media = medium`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLStyleElement/media`
 */
html.HTMLStyleElement.media = (function html$HTMLStyleElement$media(this$){
return this$.media();
});
/**
 * Property.
 * 
 *   The HTMLStyleElement.media property specifies the intended destination
 *   for style information.
 * 
 *   `medium = style.media
 *   style.media = medium`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLStyleElement/media`
 */
html.HTMLStyleElement.set_media_BANG_ = (function html$HTMLStyleElement$set_media_BANG_(this$,val){
return (this$["media"] = val);
});
/**
 * Property.
 * 
 *   The HTMLStyleElement.scoped property is a Boolean value indicating
 *   the element applies to the whole document (false) or only to
 *   parent's sub-tree (true).
 * 
 *   `value = style.scoped;
 *   style.scoped = true;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLStyleElement/scoped`
 */
html.HTMLStyleElement.scoped = (function html$HTMLStyleElement$scoped(this$){
return this$.scoped();
});
/**
 * Property.
 * 
 *   The HTMLStyleElement.scoped property is a Boolean value indicating
 *   the element applies to the whole document (false) or only to
 *   parent's sub-tree (true).
 * 
 *   `value = style.scoped;
 *   style.scoped = true;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLStyleElement/scoped`
 */
html.HTMLStyleElement.set_scoped_BANG_ = (function html$HTMLStyleElement$set_scoped_BANG_(this$,val){
return (this$["scoped"] = val);
});
/**
 * Property.
 * 
 *   The HTMLStyleElement.type read-only property returns the type
 *   the current style.
 * 
 *   `string = style.type;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLStyleElement/type`
 */
html.HTMLStyleElement.type = (function html$HTMLStyleElement$type(this$){
return this$.type();
});

//# sourceMappingURL=HTMLStyleElement.js.map?rel=1565798862144
