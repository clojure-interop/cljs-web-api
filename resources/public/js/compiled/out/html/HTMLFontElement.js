// Compiled by ClojureScript 1.9.946 {}
goog.provide('html.HTMLFontElement');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The obsolete HTMLFontElement.color property is a `dom.DOMString`
 *   reflects the color HTML attribute, containing either a named
 *   or a color specified in the hexadecimal #RRGGBB format.
 * 
 *   `colorString = fontObj.color;
 *   fontObj.color = colorString;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLFontElement/color`
 */
html.HTMLFontElement.color = (function html$HTMLFontElement$color(this$){
return this$.color();
});
/**
 * Property.
 * 
 *   The obsolete HTMLFontElement.color property is a `dom.DOMString`
 *   reflects the color HTML attribute, containing either a named
 *   or a color specified in the hexadecimal #RRGGBB format.
 * 
 *   `colorString = fontObj.color;
 *   fontObj.color = colorString;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLFontElement/color`
 */
html.HTMLFontElement.set_color_BANG_ = (function html$HTMLFontElement$set_color_BANG_(this$,val){
return (this$["color"] = val);
});
/**
 * Property.
 * 
 *   The obsolete HTMLFontElement.face property is a `dom.DOMString`
 *   reflects the face HTML attribute, containing a comma-separated
 *   of one or more font names.
 * 
 *   `faceString = fontObj.face;
 *   fontObj.face = faceString;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLFontElement/face`
 */
html.HTMLFontElement.face = (function html$HTMLFontElement$face(this$){
return this$.face();
});
/**
 * Property.
 * 
 *   The obsolete HTMLFontElement.face property is a `dom.DOMString`
 *   reflects the face HTML attribute, containing a comma-separated
 *   of one or more font names.
 * 
 *   `faceString = fontObj.face;
 *   fontObj.face = faceString;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLFontElement/face`
 */
html.HTMLFontElement.set_face_BANG_ = (function html$HTMLFontElement$set_face_BANG_(this$,val){
return (this$["face"] = val);
});
/**
 * Property.
 * 
 *   The obsolete HTMLFontElement.size property is a `dom.DOMString`
 *   reflects the size HTML attribute. It contains either an integer
 *   in the range of 1-7 or a relative value to increase/decrease
 *   value of the size attribute of the `web.<basefont>` element.
 * 
 *   `sizeString = fontObj.size;
 *   fontObj.size = sizeString;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLFontElement/size`
 */
html.HTMLFontElement.size = (function html$HTMLFontElement$size(this$){
return this$.size();
});
/**
 * Property.
 * 
 *   The obsolete HTMLFontElement.size property is a `dom.DOMString`
 *   reflects the size HTML attribute. It contains either an integer
 *   in the range of 1-7 or a relative value to increase/decrease
 *   value of the size attribute of the `web.<basefont>` element.
 * 
 *   `sizeString = fontObj.size;
 *   fontObj.size = sizeString;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLFontElement/size`
 */
html.HTMLFontElement.set_size_BANG_ = (function html$HTMLFontElement$set_size_BANG_(this$,val){
return (this$["size"] = val);
});

//# sourceMappingURL=HTMLFontElement.js.map?rel=1565798842844
