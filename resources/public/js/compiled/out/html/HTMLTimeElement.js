// Compiled by ClojureScript 1.9.946 {}
goog.provide('html.HTMLTimeElement');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The HTMLTimeElement.dateTime property is a `dom.DOMString` that
 *   the datetime HTML attribute, containing a machine-readable form
 *   the element's date and time value.
 * 
 *   `dateTimeString = timeElt.dateTime;
 *   timeElt.dateTime = dateTimeString`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLTimeElement/dateTime`
 */
html.HTMLTimeElement.date_time = (function html$HTMLTimeElement$date_time(this$){
return this$.dateTime();
});
/**
 * Property.
 * 
 *   The HTMLTimeElement.dateTime property is a `dom.DOMString` that
 *   the datetime HTML attribute, containing a machine-readable form
 *   the element's date and time value.
 * 
 *   `dateTimeString = timeElt.dateTime;
 *   timeElt.dateTime = dateTimeString`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HTMLTimeElement/dateTime`
 */
html.HTMLTimeElement.set_date_time_BANG_ = (function html$HTMLTimeElement$set_date_time_BANG_(this$,val){
return (this$["dateTime"] = val);
});

//# sourceMappingURL=HTMLTimeElement.js.map?rel=1565798810404
