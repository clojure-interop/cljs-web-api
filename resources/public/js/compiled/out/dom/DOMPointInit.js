// Compiled by ClojureScript 1.9.946 {}
goog.provide('dom.DOMPointInit');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The `dom.DOMPointInit` dictionary's w property is used to specify
 *   w perspective value of a point in space when either creating
 *   serializing to JSON a `dom.DOMPoint` or `dom.DOMPointReadOnly`
 * 
 *   `var DOMPointInit = {
 *   w: wPerspective
 *   };
 * 
 *   DOMPointInit.w = wPerspective;
 * 
 *   var wPerspective = DOMPointInit.w;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DOMPointInit/w`
 */
dom.DOMPointInit.w = (function dom$DOMPointInit$w(this$){
return this$.w();
});
/**
 * Property.
 * 
 *   The `dom.DOMPointInit` dictionary's w property is used to specify
 *   w perspective value of a point in space when either creating
 *   serializing to JSON a `dom.DOMPoint` or `dom.DOMPointReadOnly`
 * 
 *   `var DOMPointInit = {
 *   w: wPerspective
 *   };
 * 
 *   DOMPointInit.w = wPerspective;
 * 
 *   var wPerspective = DOMPointInit.w;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DOMPointInit/w`
 */
dom.DOMPointInit.set_w_BANG_ = (function dom$DOMPointInit$set_w_BANG_(this$,val){
return (this$["w"] = val);
});
/**
 * Property.
 * 
 *   The `dom.DOMPointInit` dictionary's x property is used to specify
 *   x component of a point in 2D or 3D space when either creating
 *   serializing a `dom.DOMPoint` or `dom.DOMPointReadOnly`.
 * 
 *   `var DOMPointInit = {
 *   x: xPos
 *   };
 * 
 *   DOMPointInit.x = xPos;
 * 
 *   var xPos = DOMPointInit.x;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DOMPointInit/x`
 */
dom.DOMPointInit.x = (function dom$DOMPointInit$x(this$){
return this$.x();
});
/**
 * Property.
 * 
 *   The `dom.DOMPointInit` dictionary's x property is used to specify
 *   x component of a point in 2D or 3D space when either creating
 *   serializing a `dom.DOMPoint` or `dom.DOMPointReadOnly`.
 * 
 *   `var DOMPointInit = {
 *   x: xPos
 *   };
 * 
 *   DOMPointInit.x = xPos;
 * 
 *   var xPos = DOMPointInit.x;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DOMPointInit/x`
 */
dom.DOMPointInit.set_x_BANG_ = (function dom$DOMPointInit$set_x_BANG_(this$,val){
return (this$["x"] = val);
});
/**
 * Property.
 * 
 *   The `dom.DOMPointInit` dictionary's y property is used to specify
 *   y-coordinate of a point in 2D or 3D space when either creating
 *   serializing to JSON a `dom.DOMPoint` or `dom.DOMPointReadOnly`
 * 
 *   `var DOMPointInit = {
 *   y: yPos
 *   };
 * 
 *   DOMPointInit.y = yPos;
 * 
 *   var yPos = DOMPointInit.y;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DOMPointInit/y`
 */
dom.DOMPointInit.y = (function dom$DOMPointInit$y(this$){
return this$.y();
});
/**
 * Property.
 * 
 *   The `dom.DOMPointInit` dictionary's y property is used to specify
 *   y-coordinate of a point in 2D or 3D space when either creating
 *   serializing to JSON a `dom.DOMPoint` or `dom.DOMPointReadOnly`
 * 
 *   `var DOMPointInit = {
 *   y: yPos
 *   };
 * 
 *   DOMPointInit.y = yPos;
 * 
 *   var yPos = DOMPointInit.y;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DOMPointInit/y`
 */
dom.DOMPointInit.set_y_BANG_ = (function dom$DOMPointInit$set_y_BANG_(this$,val){
return (this$["y"] = val);
});
/**
 * Property.
 * 
 *   The `dom.DOMPointInit` dictionary's z property is used to specify
 *   z-coordinate of a point in 2D or 3D space when either creating
 *   serializing to JSON a `dom.DOMPoint` or `dom.DOMPointReadOnly`
 * 
 *   `var DOMPointInit = {
 *   z: zPos
 *   };
 * 
 *   DOMPointInit.z = zPos;
 * 
 *   var zPos = DOMPointInit.z;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DOMPointInit/z`
 */
dom.DOMPointInit.z = (function dom$DOMPointInit$z(this$){
return this$.z();
});
/**
 * Property.
 * 
 *   The `dom.DOMPointInit` dictionary's z property is used to specify
 *   z-coordinate of a point in 2D or 3D space when either creating
 *   serializing to JSON a `dom.DOMPoint` or `dom.DOMPointReadOnly`
 * 
 *   `var DOMPointInit = {
 *   z: zPos
 *   };
 * 
 *   DOMPointInit.z = zPos;
 * 
 *   var zPos = DOMPointInit.z;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DOMPointInit/z`
 */
dom.DOMPointInit.set_z_BANG_ = (function dom$DOMPointInit$set_z_BANG_(this$,val){
return (this$["z"] = val);
});

//# sourceMappingURL=DOMPointInit.js.map?rel=1565798852226
