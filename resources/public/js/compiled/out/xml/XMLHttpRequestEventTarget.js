// Compiled by ClojureScript 1.9.946 {}
goog.provide('xml.XMLHttpRequestEventTarget');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onabort is the function called
 *   an `xml.XMLHttpRequest` transaction is aborted, such as when
 *   `xml.XMLHttpRequest.abort()` function is called.
 * 
 *   `XMLHttpRequest.onabort = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onabort`
 */
xml.XMLHttpRequestEventTarget.onabort = (function xml$XMLHttpRequestEventTarget$onabort(this$){
return this$.onabort();
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onabort is the function called
 *   an `xml.XMLHttpRequest` transaction is aborted, such as when
 *   `xml.XMLHttpRequest.abort()` function is called.
 * 
 *   `XMLHttpRequest.onabort = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onabort`
 */
xml.XMLHttpRequestEventTarget.set_onabort_BANG_ = (function xml$XMLHttpRequestEventTarget$set_onabort_BANG_(this$,val){
return (this$["onabort"] = val);
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onerror is the function called
 *   an `xml.XMLHttpRequest` transaction fails due to an error.
 * 
 *   `XMLHttpRequest.onerror = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onerror`
 */
xml.XMLHttpRequestEventTarget.onerror = (function xml$XMLHttpRequestEventTarget$onerror(this$){
return this$.onerror();
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onerror is the function called
 *   an `xml.XMLHttpRequest` transaction fails due to an error.
 * 
 *   `XMLHttpRequest.onerror = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onerror`
 */
xml.XMLHttpRequestEventTarget.set_onerror_BANG_ = (function xml$XMLHttpRequestEventTarget$set_onerror_BANG_(this$,val){
return (this$["onerror"] = val);
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onload is the function called when
 *   `xml.XMLHttpRequest` transaction completes successfully.
 * 
 *   `XMLHttpRequest.onload = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onload`
 */
xml.XMLHttpRequestEventTarget.onload = (function xml$XMLHttpRequestEventTarget$onload(this$){
return this$.onload();
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onload is the function called when
 *   `xml.XMLHttpRequest` transaction completes successfully.
 * 
 *   `XMLHttpRequest.onload = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onload`
 */
xml.XMLHttpRequestEventTarget.set_onload_BANG_ = (function xml$XMLHttpRequestEventTarget$set_onload_BANG_(this$,val){
return (this$["onload"] = val);
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onloadstart is the function called
 *   an `xml.XMLHttpRequest` transaction starts transferring data.
 * 
 *   `XMLHttpRequest.onloadstart = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onloadstart`
 */
xml.XMLHttpRequestEventTarget.onloadstart = (function xml$XMLHttpRequestEventTarget$onloadstart(this$){
return this$.onloadstart();
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onloadstart is the function called
 *   an `xml.XMLHttpRequest` transaction starts transferring data.
 * 
 *   `XMLHttpRequest.onloadstart = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onloadstart`
 */
xml.XMLHttpRequestEventTarget.set_onloadstart_BANG_ = (function xml$XMLHttpRequestEventTarget$set_onloadstart_BANG_(this$,val){
return (this$["onloadstart"] = val);
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onprogress is the function called
 *   with information when an `xml.XMLHttpRequest` before success
 *   .
 * 
 *   `XMLHttpRequest.onprogress = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onprogress`
 */
xml.XMLHttpRequestEventTarget.onprogress = (function xml$XMLHttpRequestEventTarget$onprogress(this$){
return this$.onprogress();
});
/**
 * Property.
 * 
 *   The XMLHttpRequestEventTarget.onprogress is the function called
 *   with information when an `xml.XMLHttpRequest` before success
 *   .
 * 
 *   `XMLHttpRequest.onprogress = callback;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequestEventTarget/onprogress`
 */
xml.XMLHttpRequestEventTarget.set_onprogress_BANG_ = (function xml$XMLHttpRequestEventTarget$set_onprogress_BANG_(this$,val){
return (this$["onprogress"] = val);
});

//# sourceMappingURL=XMLHttpRequestEventTarget.js.map?rel=1565798851984
