// Compiled by ClojureScript 1.9.946 {}
goog.provide('web.TaskAttributionTiming');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The containerId readonly property of the `web.TaskAttributionTiming`
 *   returns the container's id attribute. A container is the iframe,
 *   or object etc. that is being implicated, on the whole, for a
 *   task.
 * 
 *   `var containerId = TaskAttributionTiming.containerId;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/TaskAttributionTiming/containerId`
 */
web.TaskAttributionTiming.container_id = (function web$TaskAttributionTiming$container_id(this$){
return this$.containerId();
});
/**
 * Property.
 * 
 *   The containerId readonly property of the `web.TaskAttributionTiming`
 *   returns the container's id attribute. A container is the iframe,
 *   or object etc. that is being implicated, on the whole, for a
 *   task.
 * 
 *   `var containerId = TaskAttributionTiming.containerId;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/TaskAttributionTiming/containerId`
 */
web.TaskAttributionTiming.set_container_id_BANG_ = (function web$TaskAttributionTiming$set_container_id_BANG_(this$,val){
return (this$["containerId"] = val);
});
/**
 * Property.
 * 
 *   The containerName readonly property of the `web.TaskAttributionTiming`
 *   returns the container's name attribute. A container is the iframe,
 *   or object etc. that is being implicated, on the whole, for a
 *   task.
 * 
 *   `var containerName = TaskAttributionTiming.containerName;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/TaskAttributionTiming/containerName`
 */
web.TaskAttributionTiming.container_name = (function web$TaskAttributionTiming$container_name(this$){
return this$.containerName();
});
/**
 * Property.
 * 
 *   The containerName readonly property of the `web.TaskAttributionTiming`
 *   returns the container's name attribute. A container is the iframe,
 *   or object etc. that is being implicated, on the whole, for a
 *   task.
 * 
 *   `var containerName = TaskAttributionTiming.containerName;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/TaskAttributionTiming/containerName`
 */
web.TaskAttributionTiming.set_container_name_BANG_ = (function web$TaskAttributionTiming$set_container_name_BANG_(this$,val){
return (this$["containerName"] = val);
});
/**
 * Property.
 * 
 *   The containerSrc readonly property of the `web.TaskAttributionTiming`
 *   returns the container's src attribute. A container is the iframe,
 *   or object etc. that is being implicated, on the whole, for a
 *   task.
 * 
 *   `var containerSrc = TaskAttributionTiming.containerSrc;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/TaskAttributionTiming/containerSrc`
 */
web.TaskAttributionTiming.container_src = (function web$TaskAttributionTiming$container_src(this$){
return this$.containerSrc();
});
/**
 * Property.
 * 
 *   The containerSrc readonly property of the `web.TaskAttributionTiming`
 *   returns the container's src attribute. A container is the iframe,
 *   or object etc. that is being implicated, on the whole, for a
 *   task.
 * 
 *   `var containerSrc = TaskAttributionTiming.containerSrc;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/TaskAttributionTiming/containerSrc`
 */
web.TaskAttributionTiming.set_container_src_BANG_ = (function web$TaskAttributionTiming$set_container_src_BANG_(this$,val){
return (this$["containerSrc"] = val);
});
/**
 * Property.
 * 
 *   The containerType readonly property of the `web.TaskAttributionTiming`
 *   returns the type of frame container, one of iframe, embed, or
 * 
 *   `var containerType = TaskAttributionTiming.containerType;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/TaskAttributionTiming/containerType`
 */
web.TaskAttributionTiming.container_type = (function web$TaskAttributionTiming$container_type(this$){
return this$.containerType();
});
/**
 * Property.
 * 
 *   The containerType readonly property of the `web.TaskAttributionTiming`
 *   returns the type of frame container, one of iframe, embed, or
 * 
 *   `var containerType = TaskAttributionTiming.containerType;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/TaskAttributionTiming/containerType`
 */
web.TaskAttributionTiming.set_container_type_BANG_ = (function web$TaskAttributionTiming$set_container_type_BANG_(this$,val){
return (this$["containerType"] = val);
});

//# sourceMappingURL=TaskAttributionTiming.js.map?rel=1565798825203
