// Compiled by ClojureScript 1.9.946 {}
goog.provide('web.BasicCardResponse');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The billingAddress property of the `web.BasicCardResponse` dictionary
 *   the billing address of the card used to make the payment.
 * 
 *   `\"billingAddress\" : PaymentAddress`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/billingAddress`
 */
web.BasicCardResponse.billing_address = (function web$BasicCardResponse$billing_address(this$){
return this$.billingAddress();
});
/**
 * Property.
 * 
 *   The billingAddress property of the `web.BasicCardResponse` dictionary
 *   the billing address of the card used to make the payment.
 * 
 *   `\"billingAddress\" : PaymentAddress`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/billingAddress`
 */
web.BasicCardResponse.set_billing_address_BANG_ = (function web$BasicCardResponse$set_billing_address_BANG_(this$,val){
return (this$["billingAddress"] = val);
});
/**
 * Property.
 * 
 *   The cardNumber property of the `web.BasicCardResponse` dictionary
 *   the cardholder name of the card used to make the payment.
 * 
 *   `name = basicCardResponse.cardholderName;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/cardholderName`
 */
web.BasicCardResponse.cardholder_name = (function web$BasicCardResponse$cardholder_name(this$){
return this$.cardholderName();
});
/**
 * Property.
 * 
 *   The cardNumber property of the `web.BasicCardResponse` dictionary
 *   the cardholder name of the card used to make the payment.
 * 
 *   `name = basicCardResponse.cardholderName;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/cardholderName`
 */
web.BasicCardResponse.set_cardholder_name_BANG_ = (function web$BasicCardResponse$set_cardholder_name_BANG_(this$,val){
return (this$["cardholderName"] = val);
});
/**
 * Property.
 * 
 *   The cardNumber property of the `web.BasicCardResponse` dictionary
 *   the number of the card used to make the payment.
 * 
 *   `\"cardNumber\" : \"number\"`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/cardNumber`
 */
web.BasicCardResponse.card_number = (function web$BasicCardResponse$card_number(this$){
return this$.cardNumber();
});
/**
 * Property.
 * 
 *   The cardNumber property of the `web.BasicCardResponse` dictionary
 *   the number of the card used to make the payment.
 * 
 *   `\"cardNumber\" : \"number\"`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/cardNumber`
 */
web.BasicCardResponse.set_card_number_BANG_ = (function web$BasicCardResponse$set_card_number_BANG_(this$,val){
return (this$["cardNumber"] = val);
});
/**
 * Property.
 * 
 *   The cardSecurityCode property of the `web.BasicCardResponse`
 *   contains the security code of the card used to make the payment.
 * 
 *   `\"cardSecurityCode\" : \"number\"`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/cardSecurityCode`
 */
web.BasicCardResponse.card_security_code = (function web$BasicCardResponse$card_security_code(this$){
return this$.cardSecurityCode();
});
/**
 * Property.
 * 
 *   The cardSecurityCode property of the `web.BasicCardResponse`
 *   contains the security code of the card used to make the payment.
 * 
 *   `\"cardSecurityCode\" : \"number\"`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/cardSecurityCode`
 */
web.BasicCardResponse.set_card_security_code_BANG_ = (function web$BasicCardResponse$set_card_security_code_BANG_(this$,val){
return (this$["cardSecurityCode"] = val);
});
/**
 * Property.
 * 
 *   The expiryMonth property of the `web.BasicCardResponse` dictionary
 *   the expiry month of the card used to make the payment.
 * 
 *   `\"expiryMonth\" : \"number\"`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/expiryMonth`
 */
web.BasicCardResponse.expiry_month = (function web$BasicCardResponse$expiry_month(this$){
return this$.expiryMonth();
});
/**
 * Property.
 * 
 *   The expiryMonth property of the `web.BasicCardResponse` dictionary
 *   the expiry month of the card used to make the payment.
 * 
 *   `\"expiryMonth\" : \"number\"`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/expiryMonth`
 */
web.BasicCardResponse.set_expiry_month_BANG_ = (function web$BasicCardResponse$set_expiry_month_BANG_(this$,val){
return (this$["expiryMonth"] = val);
});
/**
 * Property.
 * 
 *   The expiryYear property of the `web.BasicCardResponse` dictionary
 *   the expiry year of the card used to make the payment.
 * 
 *   `\"expiryYear\" : \"number\"`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/expiryYear`
 */
web.BasicCardResponse.expiry_year = (function web$BasicCardResponse$expiry_year(this$){
return this$.expiryYear();
});
/**
 * Property.
 * 
 *   The expiryYear property of the `web.BasicCardResponse` dictionary
 *   the expiry year of the card used to make the payment.
 * 
 *   `\"expiryYear\" : \"number\"`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BasicCardResponse/expiryYear`
 */
web.BasicCardResponse.set_expiry_year_BANG_ = (function web$BasicCardResponse$set_expiry_year_BANG_(this$,val){
return (this$["expiryYear"] = val);
});

//# sourceMappingURL=BasicCardResponse.js.map?rel=1565798857677
