// Compiled by ClojureScript 1.9.946 {}
goog.provide('web.HashChangeEvent');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The newURL read-only property of the `web.HashChangeEvent` interface
 *   the new URL to which the window is navigating.
 * 
 *   `let newEventUrl = event.newURL;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HashChangeEvent/newURL`
 */
web.HashChangeEvent.new_url = (function web$HashChangeEvent$new_url(this$){
return this$.newURL();
});
/**
 * Property.
 * 
 *   The newURL read-only property of the `web.HashChangeEvent` interface
 *   the new URL to which the window is navigating.
 * 
 *   `let newEventUrl = event.newURL;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HashChangeEvent/newURL`
 */
web.HashChangeEvent.set_new_url_BANG_ = (function web$HashChangeEvent$set_new_url_BANG_(this$,val){
return (this$["newURL"] = val);
});
/**
 * Property.
 * 
 *   The oldURL read-only property of the `web.HashChangeEvent` interface
 *   the previous URL from which the window was navigated.
 * 
 *   `let oldEventUrl = event.oldURL;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HashChangeEvent/oldURL`
 */
web.HashChangeEvent.old_url = (function web$HashChangeEvent$old_url(this$){
return this$.oldURL();
});
/**
 * Property.
 * 
 *   The oldURL read-only property of the `web.HashChangeEvent` interface
 *   the previous URL from which the window was navigated.
 * 
 *   `let oldEventUrl = event.oldURL;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/HashChangeEvent/oldURL`
 */
web.HashChangeEvent.set_old_url_BANG_ = (function web$HashChangeEvent$set_old_url_BANG_(this$,val){
return (this$["oldURL"] = val);
});

//# sourceMappingURL=HashChangeEvent.js.map?rel=1565798811728
