// Compiled by ClojureScript 1.9.946 {}
goog.provide('web.MutationObserverInit');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional attributeFilter
 *   is an array of strings specifying the names of the attributes
 *   values are to be monitored for changes.
 * 
 *   `var options = {
 *   attributeFilter: [ \"list\", \"of\", \"attribute\", \"names\" ]
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/attributeFilter`
 */
web.MutationObserverInit.attribute_filter = (function web$MutationObserverInit$attribute_filter(this$){
return this$.attributeFilter();
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional attributeFilter
 *   is an array of strings specifying the names of the attributes
 *   values are to be monitored for changes.
 * 
 *   `var options = {
 *   attributeFilter: [ \"list\", \"of\", \"attribute\", \"names\" ]
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/attributeFilter`
 */
web.MutationObserverInit.set_attribute_filter_BANG_ = (function web$MutationObserverInit$set_attribute_filter_BANG_(this$,val){
return (this$["attributeFilter"] = val);
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional attributeOldValue
 *   is used to specify whether or not to record the prior value of
 *   altered attribute in `web.MutationRecord` objects denoting attribute
 *   changes.
 * 
 *   `var options = {
 *   attributeOldValue: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/attributeOldValue`
 */
web.MutationObserverInit.attribute_old_value = (function web$MutationObserverInit$attribute_old_value(this$){
return this$.attributeOldValue();
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional attributeOldValue
 *   is used to specify whether or not to record the prior value of
 *   altered attribute in `web.MutationRecord` objects denoting attribute
 *   changes.
 * 
 *   `var options = {
 *   attributeOldValue: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/attributeOldValue`
 */
web.MutationObserverInit.set_attribute_old_value_BANG_ = (function web$MutationObserverInit$set_attribute_old_value_BANG_(this$,val){
return (this$["attributeOldValue"] = val);
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional attributes
 *   is used to specify whether or not to watch for attribute value
 *   on the node or nodes being observed.
 * 
 *   `var options = {
 *   attributes: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/attributes`
 */
web.MutationObserverInit.attributes = (function web$MutationObserverInit$attributes(this$){
return this$.attributes();
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional attributes
 *   is used to specify whether or not to watch for attribute value
 *   on the node or nodes being observed.
 * 
 *   `var options = {
 *   attributes: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/attributes`
 */
web.MutationObserverInit.set_attributes_BANG_ = (function web$MutationObserverInit$set_attributes_BANG_(this$,val){
return (this$["attributes"] = val);
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional characterData
 *   is used to specify whether or not to monitor the node or nodes
 *   observed for changes to their textual contents.
 * 
 *   `var options = {
 *   characterData: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/characterData`
 */
web.MutationObserverInit.character_data = (function web$MutationObserverInit$character_data(this$){
return this$.characterData();
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional characterData
 *   is used to specify whether or not to monitor the node or nodes
 *   observed for changes to their textual contents.
 * 
 *   `var options = {
 *   characterData: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/characterData`
 */
web.MutationObserverInit.set_character_data_BANG_ = (function web$MutationObserverInit$set_character_data_BANG_(this$,val){
return (this$["characterData"] = val);
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional characterDataOldValue
 *   is used to specify whether or not the `web.MutationRecord.oldValue`
 *   for DOM mutations should be set to the previous value of text
 *   which changed.
 * 
 *   `var options = {
 *   characterDataOldValue: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/characterDataOldValue`
 */
web.MutationObserverInit.character_data_old_value = (function web$MutationObserverInit$character_data_old_value(this$){
return this$.characterDataOldValue();
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional characterDataOldValue
 *   is used to specify whether or not the `web.MutationRecord.oldValue`
 *   for DOM mutations should be set to the previous value of text
 *   which changed.
 * 
 *   `var options = {
 *   characterDataOldValue: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/characterDataOldValue`
 */
web.MutationObserverInit.set_character_data_old_value_BANG_ = (function web$MutationObserverInit$set_character_data_old_value_BANG_(this$,val){
return (this$["characterDataOldValue"] = val);
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional childList
 *   indicates whether or not to monitor the specified node or nodes
 *   the addition or removal of new child nodes.
 * 
 *   `var options = {
 *   childList: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/childList`
 */
web.MutationObserverInit.child_list = (function web$MutationObserverInit$child_list(this$){
return this$.childList();
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional childList
 *   indicates whether or not to monitor the specified node or nodes
 *   the addition or removal of new child nodes.
 * 
 *   `var options = {
 *   childList: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/childList`
 */
web.MutationObserverInit.set_child_list_BANG_ = (function web$MutationObserverInit$set_child_list_BANG_(this$,val){
return (this$["childList"] = val);
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional subtree
 *   can be set to true to monitor the targeted node and all of its
 * 
 *   `var options = {
 *   subtree: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/subtree`
 */
web.MutationObserverInit.subtree = (function web$MutationObserverInit$subtree(this$){
return this$.subtree();
});
/**
 * Property.
 * 
 *   The `web.MutationObserverInit` dictionary's optional subtree
 *   can be set to true to monitor the targeted node and all of its
 * 
 *   `var options = {
 *   subtree: true | false
 *   }`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/MutationObserverInit/subtree`
 */
web.MutationObserverInit.set_subtree_BANG_ = (function web$MutationObserverInit$set_subtree_BANG_(this$,val){
return (this$["subtree"] = val);
});

//# sourceMappingURL=MutationObserverInit.js.map?rel=1565798837799
