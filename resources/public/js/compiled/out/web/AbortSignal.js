// Compiled by ClojureScript 1.9.946 {}
goog.provide('web.AbortSignal');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The aborted read-only property returns a `web.Boolean` that indicates
 *   the DOM request(s) the signal is communicating with is/are aborted
 *   or not (false).
 * 
 *   `var isAborted = abortSignal.aborted;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal/aborted`
 */
web.AbortSignal.aborted = (function web$AbortSignal$aborted(this$){
return this$.aborted();
});
/**
 * Property.
 * 
 *   The aborted read-only property returns a `web.Boolean` that indicates
 *   the DOM request(s) the signal is communicating with is/are aborted
 *   or not (false).
 * 
 *   `var isAborted = abortSignal.aborted;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal/aborted`
 */
web.AbortSignal.set_aborted_BANG_ = (function web$AbortSignal$set_aborted_BANG_(this$,val){
return (this$["aborted"] = val);
});
/**
 * Property.
 * 
 *   The onabort read-only property of the `web.FetchSignal` interface
 *   an event handler Invoked when an abort event fires, i.e. when
 *   fetch request(s) the signal is communicating with is/are aborted.
 * 
 *   `abortSignal.onabort = function() { ... };`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal/onabort`
 */
web.AbortSignal.onabort = (function web$AbortSignal$onabort(this$){
return this$.onabort();
});
/**
 * Property.
 * 
 *   The onabort read-only property of the `web.FetchSignal` interface
 *   an event handler Invoked when an abort event fires, i.e. when
 *   fetch request(s) the signal is communicating with is/are aborted.
 * 
 *   `abortSignal.onabort = function() { ... };`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal/onabort`
 */
web.AbortSignal.set_onabort_BANG_ = (function web$AbortSignal$set_onabort_BANG_(this$,val){
return (this$["onabort"] = val);
});

//# sourceMappingURL=AbortSignal.js.map?rel=1565798813159
