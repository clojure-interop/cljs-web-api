// Compiled by ClojureScript 1.9.946 {}
goog.provide('web.BudgetState');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The budgetAt read-only property of the `web.BudgetState` interface
 *   the anticipated processing budget at the specified time.
 * 
 *   `var budget = BudgetState.budgetAt`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BudgetState/budgetAt`
 */
web.BudgetState.budget_at = (function web$BudgetState$budget_at(this$){
return this$.budgetAt();
});
/**
 * Property.
 * 
 *   The budgetAt read-only property of the `web.BudgetState` interface
 *   the anticipated processing budget at the specified time.
 * 
 *   `var budget = BudgetState.budgetAt`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BudgetState/budgetAt`
 */
web.BudgetState.set_budget_at_BANG_ = (function web$BudgetState$set_budget_at_BANG_(this$,val){
return (this$["budgetAt"] = val);
});
/**
 * Property.
 * 
 *   The time read-only property of the `web.BudgetState` interface
 *   a `web.timestamp` at which the budgetAt value is valid.
 * 
 *   `var time = BudgetState.time`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BudgetState/time`
 */
web.BudgetState.time = (function web$BudgetState$time(this$){
return this$.time();
});
/**
 * Property.
 * 
 *   The time read-only property of the `web.BudgetState` interface
 *   a `web.timestamp` at which the budgetAt value is valid.
 * 
 *   `var time = BudgetState.time`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/BudgetState/time`
 */
web.BudgetState.set_time_BANG_ = (function web$BudgetState$set_time_BANG_(this$,val){
return (this$["time"] = val);
});

//# sourceMappingURL=BudgetState.js.map?rel=1565798836839
