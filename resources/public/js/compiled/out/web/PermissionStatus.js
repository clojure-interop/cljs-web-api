// Compiled by ClojureScript 1.9.946 {}
goog.provide('web.PermissionStatus');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   The onchange event handler of the `web.PermissionStatus` interface
 *   called whenever the `web.PermissionStatus.state` property changes.
 * 
 *   `PermissionStatus.onchange = function() { ... }
 *   PermissionStatus.addEventListener('change', function() { ... })`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/PermissionStatus/onchange`
 */
web.PermissionStatus.onchange = (function web$PermissionStatus$onchange(this$){
return this$.onchange();
});
/**
 * Property.
 * 
 *   The onchange event handler of the `web.PermissionStatus` interface
 *   called whenever the `web.PermissionStatus.state` property changes.
 * 
 *   `PermissionStatus.onchange = function() { ... }
 *   PermissionStatus.addEventListener('change', function() { ... })`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/PermissionStatus/onchange`
 */
web.PermissionStatus.set_onchange_BANG_ = (function web$PermissionStatus$set_onchange_BANG_(this$,val){
return (this$["onchange"] = val);
});
/**
 * Property.
 * 
 *   The state read-only property of the `web.PermissionStatus` interface
 *   the state of a requested permission. This property returns one
 *   'granted', 'denied', or 'prompt'.
 * 
 *   `var permission = PermissionStatus.state;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/PermissionStatus/state`
 */
web.PermissionStatus.state = (function web$PermissionStatus$state(this$){
return this$.state();
});
/**
 * Property.
 * 
 *   The state read-only property of the `web.PermissionStatus` interface
 *   the state of a requested permission. This property returns one
 *   'granted', 'denied', or 'prompt'.
 * 
 *   `var permission = PermissionStatus.state;`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/PermissionStatus/state`
 */
web.PermissionStatus.set_state_BANG_ = (function web$PermissionStatus$set_state_BANG_(this$,val){
return (this$["state"] = val);
});

//# sourceMappingURL=PermissionStatus.js.map?rel=1565798811960
