// Compiled by ClojureScript 1.9.946 {}
goog.provide('web.DeviceRotationRate');
goog.require('cljs.core');
/**
 * Property.
 * 
 *   This property indicates the rate of rotation around the Z axis
 *   in degrees per second -- in a `web.DeviceRotationRate` object.
 * 
 *   `var alpha = deviceRotationRate.alpha;
 * 
 *   This property is read-only.`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DeviceRotationRate/alpha`
 */
web.DeviceRotationRate.alpha = (function web$DeviceRotationRate$alpha(this$){
return this$.alpha();
});
/**
 * Property.
 * 
 *   This property indicates the rate of rotation around the Z axis
 *   in degrees per second -- in a `web.DeviceRotationRate` object.
 * 
 *   `var alpha = deviceRotationRate.alpha;
 * 
 *   This property is read-only.`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DeviceRotationRate/alpha`
 */
web.DeviceRotationRate.set_alpha_BANG_ = (function web$DeviceRotationRate$set_alpha_BANG_(this$,val){
return (this$["alpha"] = val);
});
/**
 * Property.
 * 
 *   This property indicates the rate of rotation around the X axis
 *   in degrees per second -- in a `web.DeviceRotationRate` object.
 * 
 *   `var beta = deviceRotationRate.beta;
 * 
 *   This property is read-only.`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DeviceRotationRate/beta`
 */
web.DeviceRotationRate.beta = (function web$DeviceRotationRate$beta(this$){
return this$.beta();
});
/**
 * Property.
 * 
 *   This property indicates the rate of rotation around the X axis
 *   in degrees per second -- in a `web.DeviceRotationRate` object.
 * 
 *   `var beta = deviceRotationRate.beta;
 * 
 *   This property is read-only.`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DeviceRotationRate/beta`
 */
web.DeviceRotationRate.set_beta_BANG_ = (function web$DeviceRotationRate$set_beta_BANG_(this$,val){
return (this$["beta"] = val);
});
/**
 * Property.
 * 
 *   This property indicates the rate of rotation around the Y axis
 *   in degrees per second -- in a `web.DeviceRotationRate` object.
 * 
 *   `var gamma = deviceRotationRate.gamma;
 * 
 *   This property is read-only.`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DeviceRotationRate/gamma`
 */
web.DeviceRotationRate.gamma = (function web$DeviceRotationRate$gamma(this$){
return this$.gamma();
});
/**
 * Property.
 * 
 *   This property indicates the rate of rotation around the Y axis
 *   in degrees per second -- in a `web.DeviceRotationRate` object.
 * 
 *   `var gamma = deviceRotationRate.gamma;
 * 
 *   This property is read-only.`
 * 
 *   See also: `https://developer.mozilla.org/en-US/docs/Web/API/DeviceRotationRate/gamma`
 */
web.DeviceRotationRate.set_gamma_BANG_ = (function web$DeviceRotationRate$set_gamma_BANG_(this$,val){
return (this$["gamma"] = val);
});

//# sourceMappingURL=DeviceRotationRate.js.map?rel=1565798810253
