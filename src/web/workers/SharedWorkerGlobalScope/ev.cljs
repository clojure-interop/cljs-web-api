(ns web.workers.SharedWorkerGlobalScope.ev
  "SharedWorkerGlobalScope Events."
  (:refer-clojure :exclude []))

(def connect
  "Event.

  Fired on shared workers when a new client connects.
  Also available via the onconnect property."

  "connect")

