(ns web.credential.core
  "web.credential interfaces."
  (:require [web.credential.Credential]
            [web.credential.CredentialsContainer]
            [web.credential.FederatedCredential]
            [web.credential.PasswordCredential]))
