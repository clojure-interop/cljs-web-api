(ns web.speech.SpeechSynthesis.ev
  "SpeechSynthesis Events."
  (:refer-clojure :exclude []))

(def voiceschanged
  "Event.

  Fired when the list of SpeechSynthesisVoice objects that would
  returned by the SpeechSynthesis.getVoices() method has changed.
  Also available via the onvoiceschanged property."

  "voiceschanged")

