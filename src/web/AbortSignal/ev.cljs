(ns web.AbortSignal.ev
  "AbortSignal Events."
  (:refer-clojure :exclude []))

(def abort
  "Event.

  Invoked when the DOM request(s) the signal is communicating with
  aborted.
  Also available via the onabort property."

  "abort")

