(ns web.svg.SVGFESpecularLightingElement
  "The SVGFESpecularLightingElement interface corresponds to the
  element."
  (:refer-clojure :exclude []))

(defn in1
  "Property.

  An SVGAnimatedString corresponding to the in attribute of the
  element."
  [this]
  (-> this (.-in1)))

(defn set-in1!
  "Property.

  An SVGAnimatedString corresponding to the in attribute of the
  element."
  [this val]
  (aset this "in1" val))

(defn surface-scale
  "Property.

  An SVGAnimatedNumber corresponding to the surfaceScale attribute
  the given element."
  [this]
  (-> this (.-surfaceScale)))

(defn set-surface-scale!
  "Property.

  An SVGAnimatedNumber corresponding to the surfaceScale attribute
  the given element."
  [this val]
  (aset this "surfaceScale" val))

(defn specular-constant
  "Property.

  An SVGAnimatedNumber corresponding to the specularConstant attribute
  the given element."
  [this]
  (-> this (.-specularConstant)))

(defn set-specular-constant!
  "Property.

  An SVGAnimatedNumber corresponding to the specularConstant attribute
  the given element."
  [this val]
  (aset this "specularConstant" val))

(defn specular-exponent
  "Property.

  An SVGAnimatedNumber corresponding to the specularExponent attribute
  the given element."
  [this]
  (-> this (.-specularExponent)))

(defn set-specular-exponent!
  "Property.

  An SVGAnimatedNumber corresponding to the specularExponent attribute
  the given element."
  [this val]
  (aset this "specularExponent" val))

(defn kernel-unit-length-x
  "Property.

  An SVGAnimatedNumber corresponding to the X component of the
  attribute of the given element."
  [this]
  (-> this (.-kernelUnitLengthX)))

(defn set-kernel-unit-length-x!
  "Property.

  An SVGAnimatedNumber corresponding to the X component of the
  attribute of the given element."
  [this val]
  (aset this "kernelUnitLengthX" val))

(defn kernel-unit-length-y
  "Property.

  An SVGAnimatedNumber corresponding to the Y component of the
  attribute of the given element."
  [this]
  (-> this (.-kernelUnitLengthY)))

(defn set-kernel-unit-length-y!
  "Property.

  An SVGAnimatedNumber corresponding to the Y component of the
  attribute of the given element."
  [this val]
  (aset this "kernelUnitLengthY" val))

