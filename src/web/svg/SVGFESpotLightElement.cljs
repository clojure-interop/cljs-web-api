(ns web.svg.SVGFESpotLightElement
  "The SVGFESpotLightElement interface corresponds to the `<feSpotLight>`"
  (:refer-clojure :exclude []))

(defn x
  "Property.

  An SVGAnimatedNumber corresponding to the x attribute of the
  element."
  [this]
  (-> this (.-x)))

(defn set-x!
  "Property.

  An SVGAnimatedNumber corresponding to the x attribute of the
  element."
  [this val]
  (aset this "x" val))

(defn y
  "Property.

  An SVGAnimatedNumber corresponding to the y attribute of the
  element."
  [this]
  (-> this (.-y)))

(defn set-y!
  "Property.

  An SVGAnimatedNumber corresponding to the y attribute of the
  element."
  [this val]
  (aset this "y" val))

(defn z
  "Property.

  An SVGAnimatedNumber corresponding to the z attribute of the
  element."
  [this]
  (-> this (.-z)))

(defn set-z!
  "Property.

  An SVGAnimatedNumber corresponding to the z attribute of the
  element."
  [this val]
  (aset this "z" val))

(defn point-at-x
  "Property.

  An SVGAnimatedNumber corresponding to the pointAtX attribute
  the given element."
  [this]
  (-> this (.-pointAtX)))

(defn set-point-at-x!
  "Property.

  An SVGAnimatedNumber corresponding to the pointAtX attribute
  the given element."
  [this val]
  (aset this "pointAtX" val))

(defn point-at-y
  "Property.

  An SVGAnimatedNumber corresponding to the pointAtY attribute
  the given element."
  [this]
  (-> this (.-pointAtY)))

(defn set-point-at-y!
  "Property.

  An SVGAnimatedNumber corresponding to the pointAtY attribute
  the given element."
  [this val]
  (aset this "pointAtY" val))

(defn point-at-z
  "Property.

  An SVGAnimatedNumber corresponding to the pointAtZ attribute
  the given element."
  [this]
  (-> this (.-pointAtZ)))

(defn set-point-at-z!
  "Property.

  An SVGAnimatedNumber corresponding to the pointAtZ attribute
  the given element."
  [this val]
  (aset this "pointAtZ" val))

(defn specular-exponent
  "Property.

  An SVGAnimatedNumber corresponding to the specularExponent attribute
  the given element."
  [this]
  (-> this (.-specularExponent)))

(defn set-specular-exponent!
  "Property.

  An SVGAnimatedNumber corresponding to the specularExponent attribute
  the given element."
  [this val]
  (aset this "specularExponent" val))

(defn limiting-cone-angle
  "Property.

  An SVGAnimatedNumber corresponding to the limitingConeAngle attribute
  the given element."
  [this]
  (-> this (.-limitingConeAngle)))

(defn set-limiting-cone-angle!
  "Property.

  An SVGAnimatedNumber corresponding to the limitingConeAngle attribute
  the given element."
  [this val]
  (aset this "limitingConeAngle" val))

